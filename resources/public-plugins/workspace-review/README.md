# Workspace Review

Workspace Review provides a read-only review skill and reviewer agent for checking implementation quality and verification coverage.

The package is intentionally declarative and does not execute commands or connect to external services by itself.
