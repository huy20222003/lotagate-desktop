# Browser Use

Browser Use lets LotaGate browse and interact with web pages through its
governed Desktop browser runtime. The plugin contains agent guidance only;
Desktop owns browser-session isolation, navigation policy, origin allowlists,
approval, file transfer, screenshots, and evidence retention.

Additional capabilities include bounded HTML table extraction, frame discovery,
drag-and-drop, and page-to-PDF export.
