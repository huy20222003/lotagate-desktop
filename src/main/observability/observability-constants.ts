export const LOG_RETENTION_DAYS = 30;
export const MAX_STRING_LENGTH = 1_024;
export const REDACTED = '[REDACTED]';
export const SENSITIVE_KEY = /(authorization|cookie|credential|password|passphrase|secret|token|api[-_]?key|private[-_]?key|file[-_]?data|prompt|content|base64)/iu;
export const LOG_FILE_PATTERN = /^desktop-(\d{4}-\d{2}-\d{2})\.log$/u;
